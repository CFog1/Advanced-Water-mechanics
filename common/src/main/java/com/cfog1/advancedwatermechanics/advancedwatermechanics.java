package com.cfog1.advancedwatermechanics;

import net.minecraft.world.entity.player.Player;
import net.minecraft.world.level.Level;

public class advancedwatermechanics  {

    public void onInitialize() {
        //BlockBreakEvents.PLAYER_BLOCK_BREAK.register((world, player, pos, state, entity) -> {
//            if (player.isUnderWater()) {
//                movement(player, world);
            }
//        });
//    }

//    private void movement(Player player, Level level) {
//        player.setSwimming(0.02F);
//        player.setAirSupply(Player.getAirSupply() - 1);
//        if (player.getAirSupply() <= 0) {
//            player.hurt(player.damageSources().drown(), 1F);
        }

